import { PokeFormatPipe } from './poke-format.pipe';

describe('PokeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PokeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
